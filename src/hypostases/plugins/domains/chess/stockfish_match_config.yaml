# HYPOSTASES Domain Plugin: Chess Ground B Stockfish Benchmark Configuration
# Spec Ref: Rule 006 (Data-driven YAML Configuration for domain plugins under plugin asset paths)

dataset:
  num_positions: 2000
  seed: 42

training:
  epochs: 50
  learning_rate: 0.001
  seed: 1488

match:
  target_elo: 1400.0
  games: 8
  time_control_sec: 0.02
  max_workers: 20
  stockfish_threads: 2

search:
  max_depth: 4
  time_budget_ms: 50.0
  tt_size_mb: 16
  quiescence_depth: 4
